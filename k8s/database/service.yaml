apiVersion: v1
kind: Service
metadata:
  name: postgres-svc
  namespace: notes-app
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None   # headless for StatefulSet
